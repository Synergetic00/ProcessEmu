package types;

import javafx.scene.canvas.GraphicsContext;
import main.FXApp;

public class PGraphicsOpenGL extends PGraphics {

    public PGraphicsOpenGL(GraphicsContext gc, FXApp parent) {
        super(gc, parent);
    }
    
}